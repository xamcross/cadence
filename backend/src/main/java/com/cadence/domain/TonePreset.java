package com.cadence.domain;

/** Code-shipped starter-wording presets (F21 D10). Applying one replaces the override's subject+body. */
public enum TonePreset {
    FORMAL,
    FRIENDLY,
    CONCISE
}
